# Polly development dependencies
# Run: brew bundle

# Secret detection — wired into bun check secrets and the pre-commit hook
brew "gitleaks"

# Static application security testing (SAST) — wired into bun check security
brew "semgrep"

# Dependency vulnerability scanning (npm + Dockerfile base images)
# — wired into bun check deps-audit
brew "osv-scanner"
