{
  "name": "server",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "NODE_ENV=production bun src/index.ts",
    "build": "echo 'No build step for server'",
    "test": "bun test",
    "typecheck": "bun tsc --noEmit",
    "verify": "bun run src/verify.ts",
    "verify:gen": "bun run src/verify.ts"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.2.0",
    "@fairfox/polly": "workspace:*",
    "@preact/signals-core": "^2.5.1",
    "elysia": "^1.4.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "typescript": "^5.9.3"
  }
}
