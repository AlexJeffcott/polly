{
  "name": "showcase-example",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Visual gallery of every @fairfox/polly UI primitive. Acts as the team's UI testing playground: visual regression baselines + browser unit tests live alongside the components they exercise.",
  "scripts": {
    "dev": "bun scripts/dev.ts",
    "typecheck": "tsc --noEmit",
    "lint": "biome lint src tests scripts",
    "format": "biome format --write src tests scripts",
    "test:browser": "polly test:browser tests/browser",
    "test:visual": "bun tests/visual/run.ts",
    "test:visual:update": "POLLY_VISUAL_UPDATE=1 bun tests/visual/run.ts",
    "test:a11y": "bun tests/a11y/run.ts",
    "test": "bun run test:browser && bun run test:visual && bun run test:a11y",
    "test:all": "bun run lint && bun run typecheck && bun run test"
  },
  "dependencies": {
    "@fairfox/polly": "workspace:*",
    "preact": "10.29.1",
    "@preact/signals": "2.9.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.14",
    "@types/bun": "1.3.13",
    "axe-core": "4.10.3",
    "puppeteer": "24.42.0",
    "typescript": "5.9.3"
  }
}
