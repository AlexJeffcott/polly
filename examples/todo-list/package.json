{
  "name": "@fairfox/todo-list",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "polly check",
    "dev": "polly dev",
    "build": "polly build",
    "build:prod": "polly build --prod",
    "typecheck": "polly typecheck",
    "lint": "polly lint",
    "lint:fix": "polly lint --fix",
    "format": "polly format",
    "test": "polly test",
    "test:all": "polly lint && polly typecheck && polly test && polly verify",
    "verify": "polly verify",
    "verify:setup": "polly verify --setup",
    "visualize": "polly visualize",
    "visualize:export": "polly visualize --export",
    "visualize:serve": "polly visualize --serve",
    "teach": "polly teach"
  },
  "dependencies": {
    "@fairfox/polly": "^0.7",
    "preact": "^10",
    "@preact/signals": "^2"
  },
  "devDependencies": {
    "@types/chrome": "^0.1",
    "bun-types": "latest"
  }
}
