{
  "name": "@fairfox/verify-websocket-example",
  "version": "1.0.0",
  "private": true,
  "description": "WebSocket chat + todo app with formal verification",
  "scripts": {
    "start": "bun run src/server.ts",
    "test": "bun run test-harness.ts",
    "dev": "bun run --watch src/server.ts"
  },
  "dependencies": {
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
